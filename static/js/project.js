


class Project {
    constructor (config) {
        console.log(config)
        this.config = config
    }

    render(options) {
        console.log(options)
        this.options = options
        
    }
}